interface:
  display_name: "Codex Plan Task Workflow"
  short_description: "Plan-to-tasks workflow for Codex implementation turns"
  default_prompt: "Use $codex-plan-task-workflow before implementing or applying a coding plan. Create or reuse tasks/..., write plan.md and tasks.md before editing, execute the checklist, verify, and record learning candidates in result.md."

policy:
  allow_implicit_invocation: true
